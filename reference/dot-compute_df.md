# Compute the degree of freedom

This is an intermediary function used in `compute_pvalue`

## Usage

``` r
.compute_df(input_obj)
```

## Arguments

- input_obj:

  `eSVD` object outputed from `compute_test_statistic`.

## Value

a named vector of degree-of-freedom values, one for each gene
